sudo: required
dist: trusty
stages:
  - test
  - lint
language: python
python:
  - "2.7"
  - "3.6"
env:
  - COMPONENT=knn
install:
- pip install -r requirements.txt
- sudo apt-get install python-scipy
script:
- cd $COMPONENT
- pytest
git:
  depth:
    3
jobs:
  include:
  - stage: lint
    env:
    language: python
    python: 3.6
    install:
    - pip install pylint
    script:
    - pylint 
